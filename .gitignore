# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Local env files (keep the example tracked)
.env
.env.*
!.env.example

# Local binaries (yt-dlp, ffmpeg) for the video pipeline — never commit
tools/

# Local Netlify folder
.netlify

# Generated iOS Shortcut — contains the SHORTCUT_TOKEN secret, never commit
*.shortcut

# iOS signing keys — NEVER commit these
*.pem
ios_distribution_key*

# store-screenshot generator working dirs (source zip lives in Downloads)
scripts/pics/
scripts/store-6.5/
scripts/store-6.9/
